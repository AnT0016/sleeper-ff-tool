"""In-house models over the Phase 8 lake (Phase 9).

The evaluation harness (:mod:`model.evaluate`) and the naive baselines (:mod:`model.baselines`) ship
first, by design: with no historical Sleeper projection to beat (``baseline_sleeper_points`` is null
for all of 2016-2025), "better" has no definition until the bar is set. See
``docs/plans/modeling.md`` (Decision #1).
"""
